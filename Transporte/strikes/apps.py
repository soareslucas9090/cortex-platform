from django.apps import AppConfig


class StrikesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Transporte.strikes'
    verbose_name = 'Transporte — Strikes'

